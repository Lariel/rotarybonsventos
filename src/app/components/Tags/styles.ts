import styled from 'styled-components';

export const TagsContainerStyled = styled.div`
	padding: 10px;
	display: flex;
	flex-wrap: wrap;
	width: 100%;
	align-items: center;
	position: relative;
	overflow: hidden;
`;
